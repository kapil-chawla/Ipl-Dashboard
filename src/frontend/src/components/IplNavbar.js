import React from 'react';

export const IplNavbar = () => {
    return (
        <nav style={{backgroundColor:'transparent'}}>
            <h1 style={{textAlign:'center'}}>
                <a href='/'>Ipl Dashboard</a>
            </h1>
        </nav>
    )
}
